name: Actualizar archivo de texto con nombres de archivos
on:
  push:
    branches: [ main ]
jobs:
  actualizar-archivo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Actualizar archivo de texto
        run: |
          python script.py # Inserta aquí el comando o el script que lea los nombres de archivos en la carpeta deseada y los escriba en un archivo de texto
          git config --global user.email "tu-email@ejemplo.com"
          git config --global user.name "Tu nombre"
          git add nombres_archivos.txt # Cambia el nombre del archivo si lo has llamado de
